// <auto-generated />
#nullable enable

using System;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace ZYC.Codex.Protocol;

public class SubAgentSourceJsonConverter : JsonConverter<SubAgentSource>
{
    public override SubAgentSource Read(ref Utf8JsonReader reader, Type typeToConvert, JsonSerializerOptions options)
    {
        using var document = JsonDocument.ParseValue(ref reader);
        var root = document.RootElement;
        if (root.ValueKind == JsonValueKind.String && root.GetString() == "compact")
            return new SubAgentSourceCompact();
        if (root.ValueKind == JsonValueKind.String && root.GetString() == "memory_consolidation")
            return new SubAgentSourceMemoryConsolidation();
        if (root.ValueKind == JsonValueKind.Object && root.TryGetProperty("other", out var property0))
            return root.Deserialize<OtherSubAgentSource>(options) ?? throw new JsonException("Expected OtherSubAgentSource.");
        if (root.ValueKind == JsonValueKind.String && root.GetString() == "review")
            return new SubAgentSourceReview();
        if (root.ValueKind == JsonValueKind.Object && root.TryGetProperty("thread_spawn", out var property1))
            return root.Deserialize<ThreadSpawnSubAgentSource>(options) ?? throw new JsonException("Expected ThreadSpawnSubAgentSource.");
        throw new JsonException("Unknown SubAgentSource wire shape or value.");
    }

    public override void Write(Utf8JsonWriter writer, SubAgentSource value, JsonSerializerOptions options)
    {
        switch (value)
        {
            case SubAgentSourceCompact concrete:
                writer.WriteStringValue("compact");
                break;
            case SubAgentSourceMemoryConsolidation concrete:
                writer.WriteStringValue("memory_consolidation");
                break;
            case OtherSubAgentSource concrete:
                JsonSerializer.Serialize(writer, concrete, options);
                break;
            case SubAgentSourceReview concrete:
                writer.WriteStringValue("review");
                break;
            case ThreadSpawnSubAgentSource concrete:
                JsonSerializer.Serialize(writer, concrete, options);
                break;
            default: throw new JsonException("Unknown runtime type for SubAgentSource.");
        }
    }
}
