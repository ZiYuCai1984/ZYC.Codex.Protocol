// <auto-generated />
#nullable enable

using System;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace ZYC.Codex.Protocol;

public class ReviewTargetJsonConverter : JsonConverter<ReviewTarget>
{
    public override ReviewTarget Read(ref Utf8JsonReader reader, Type typeToConvert, JsonSerializerOptions options)
    {
        using var document = JsonDocument.ParseValue(ref reader);
        var root = document.RootElement;
        if (root.ValueKind != JsonValueKind.Object || !root.TryGetProperty("type", out var discriminator) || discriminator.ValueKind != JsonValueKind.String)
            throw new JsonException("Missing or invalid ReviewTarget type.");
        return discriminator.GetString() switch
        {
            "baseBranch" => root.Deserialize<BaseBranchReviewTarget>(options) ?? throw new JsonException("Expected BaseBranchReviewTarget."),
            "commit" => root.Deserialize<CommitReviewTarget>(options) ?? throw new JsonException("Expected CommitReviewTarget."),
            "custom" => root.Deserialize<CustomReviewTarget>(options) ?? throw new JsonException("Expected CustomReviewTarget."),
            "uncommittedChanges" => root.Deserialize<UncommittedChangesReviewTarget>(options) ?? throw new JsonException("Expected UncommittedChangesReviewTarget."),
            _ => throw new JsonException("Unknown ReviewTarget type \u0027" + discriminator.GetString() + "\u0027.")
        };
    }

    public override void Write(Utf8JsonWriter writer, ReviewTarget value, JsonSerializerOptions options)
    {
        switch (value)
        {
            case BaseBranchReviewTarget concrete:
                JsonSerializer.Serialize(writer, concrete, options);
                break;
            case CommitReviewTarget concrete:
                JsonSerializer.Serialize(writer, concrete, options);
                break;
            case CustomReviewTarget concrete:
                JsonSerializer.Serialize(writer, concrete, options);
                break;
            case UncommittedChangesReviewTarget concrete:
                JsonSerializer.Serialize(writer, concrete, options);
                break;
            default: throw new JsonException("Unknown runtime type for ReviewTarget.");
        }
    }
}
