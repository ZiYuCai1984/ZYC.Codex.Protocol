// <auto-generated />
#nullable enable

using System;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace ZYC.Codex.Protocol;

public class SessionSourceJsonConverter : JsonConverter<SessionSource>
{
    public override SessionSource Read(ref Utf8JsonReader reader, Type typeToConvert, JsonSerializerOptions options)
    {
        using var document = JsonDocument.ParseValue(ref reader);
        var root = document.RootElement;
        if (root.ValueKind == JsonValueKind.String && root.GetString() == "appServer")
            return new SessionSourceAppServer();
        if (root.ValueKind == JsonValueKind.String && root.GetString() == "cli")
            return new SessionSourceCli();
        if (root.ValueKind == JsonValueKind.Object && root.TryGetProperty("custom", out var property0))
            return root.Deserialize<CustomSessionSource>(options) ?? throw new JsonException("Expected CustomSessionSource.");
        if (root.ValueKind == JsonValueKind.String && root.GetString() == "exec")
            return new SessionSourceExec();
        if (root.ValueKind == JsonValueKind.Object && root.TryGetProperty("subAgent", out var property1))
            return root.Deserialize<SubAgentSessionSource>(options) ?? throw new JsonException("Expected SubAgentSessionSource.");
        if (root.ValueKind == JsonValueKind.String && root.GetString() == "unknown")
            return new SessionSourceUnknown();
        if (root.ValueKind == JsonValueKind.String && root.GetString() == "vscode")
            return new SessionSourceVscode();
        throw new JsonException("Unknown SessionSource wire shape or value.");
    }

    public override void Write(Utf8JsonWriter writer, SessionSource value, JsonSerializerOptions options)
    {
        switch (value)
        {
            case SessionSourceAppServer concrete:
                writer.WriteStringValue("appServer");
                break;
            case SessionSourceCli concrete:
                writer.WriteStringValue("cli");
                break;
            case CustomSessionSource concrete:
                JsonSerializer.Serialize(writer, concrete, options);
                break;
            case SessionSourceExec concrete:
                writer.WriteStringValue("exec");
                break;
            case SubAgentSessionSource concrete:
                JsonSerializer.Serialize(writer, concrete, options);
                break;
            case SessionSourceUnknown concrete:
                writer.WriteStringValue("unknown");
                break;
            case SessionSourceVscode concrete:
                writer.WriteStringValue("vscode");
                break;
            default: throw new JsonException("Unknown runtime type for SessionSource.");
        }
    }
}
