// <auto-generated />
#nullable enable

using System;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace ZYC.Codex.Protocol;

public class UserInputJsonConverter : JsonConverter<UserInput>
{
    public override UserInput Read(ref Utf8JsonReader reader, Type typeToConvert, JsonSerializerOptions options)
    {
        using var document = JsonDocument.ParseValue(ref reader);
        var root = document.RootElement;
        if (root.ValueKind != JsonValueKind.Object || !root.TryGetProperty("type", out var discriminator) || discriminator.ValueKind != JsonValueKind.String)
            throw new JsonException("Missing or invalid UserInput type.");
        return discriminator.GetString() switch
        {
            "audio" => root.Deserialize<AudioUserInput>(options) ?? throw new JsonException("Expected AudioUserInput."),
            "image" => root.Deserialize<ImageUserInput>(options) ?? throw new JsonException("Expected ImageUserInput."),
            "localAudio" => root.Deserialize<LocalAudioUserInput>(options) ?? throw new JsonException("Expected LocalAudioUserInput."),
            "localImage" => root.Deserialize<LocalImageUserInput>(options) ?? throw new JsonException("Expected LocalImageUserInput."),
            "mention" => root.Deserialize<MentionUserInput>(options) ?? throw new JsonException("Expected MentionUserInput."),
            "skill" => root.Deserialize<SkillUserInput>(options) ?? throw new JsonException("Expected SkillUserInput."),
            "text" => root.Deserialize<TextUserInput>(options) ?? throw new JsonException("Expected TextUserInput."),
            _ => throw new JsonException("Unknown UserInput type \u0027" + discriminator.GetString() + "\u0027.")
        };
    }

    public override void Write(Utf8JsonWriter writer, UserInput value, JsonSerializerOptions options)
    {
        switch (value)
        {
            case AudioUserInput concrete:
                JsonSerializer.Serialize(writer, concrete, options);
                break;
            case ImageUserInput concrete:
                JsonSerializer.Serialize(writer, concrete, options);
                break;
            case LocalAudioUserInput concrete:
                JsonSerializer.Serialize(writer, concrete, options);
                break;
            case LocalImageUserInput concrete:
                JsonSerializer.Serialize(writer, concrete, options);
                break;
            case MentionUserInput concrete:
                JsonSerializer.Serialize(writer, concrete, options);
                break;
            case SkillUserInput concrete:
                JsonSerializer.Serialize(writer, concrete, options);
                break;
            case TextUserInput concrete:
                JsonSerializer.Serialize(writer, concrete, options);
                break;
            default: throw new JsonException("Unknown runtime type for UserInput.");
        }
    }
}
