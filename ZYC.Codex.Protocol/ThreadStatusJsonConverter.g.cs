// <auto-generated />
#nullable enable

using System;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace ZYC.Codex.Protocol;

public class ThreadStatusJsonConverter : JsonConverter<ThreadStatus>
{
    public override ThreadStatus Read(ref Utf8JsonReader reader, Type typeToConvert, JsonSerializerOptions options)
    {
        using var document = JsonDocument.ParseValue(ref reader);
        var root = document.RootElement;
        if (root.ValueKind != JsonValueKind.Object || !root.TryGetProperty("type", out var discriminator) || discriminator.ValueKind != JsonValueKind.String)
            throw new JsonException("Missing or invalid ThreadStatus type.");
        return discriminator.GetString() switch
        {
            "active" => root.Deserialize<ActiveThreadStatus>(options) ?? throw new JsonException("Expected ActiveThreadStatus."),
            "idle" => root.Deserialize<IdleThreadStatus>(options) ?? throw new JsonException("Expected IdleThreadStatus."),
            "notLoaded" => root.Deserialize<NotLoadedThreadStatus>(options) ?? throw new JsonException("Expected NotLoadedThreadStatus."),
            "systemError" => root.Deserialize<SystemErrorThreadStatus>(options) ?? throw new JsonException("Expected SystemErrorThreadStatus."),
            _ => throw new JsonException("Unknown ThreadStatus type \u0027" + discriminator.GetString() + "\u0027.")
        };
    }

    public override void Write(Utf8JsonWriter writer, ThreadStatus value, JsonSerializerOptions options)
    {
        switch (value)
        {
            case ActiveThreadStatus concrete:
                JsonSerializer.Serialize(writer, concrete, options);
                break;
            case IdleThreadStatus concrete:
                JsonSerializer.Serialize(writer, concrete, options);
                break;
            case NotLoadedThreadStatus concrete:
                JsonSerializer.Serialize(writer, concrete, options);
                break;
            case SystemErrorThreadStatus concrete:
                JsonSerializer.Serialize(writer, concrete, options);
                break;
            default: throw new JsonException("Unknown runtime type for ThreadStatus.");
        }
    }
}
