// <auto-generated />
#nullable enable

using System;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace ZYC.Codex.Protocol;

public class CommandActionJsonConverter : JsonConverter<CommandAction>
{
    public override CommandAction Read(ref Utf8JsonReader reader, Type typeToConvert, JsonSerializerOptions options)
    {
        using var document = JsonDocument.ParseValue(ref reader);
        var root = document.RootElement;
        if (root.ValueKind != JsonValueKind.Object || !root.TryGetProperty("type", out var discriminator) || discriminator.ValueKind != JsonValueKind.String)
            throw new JsonException("Missing or invalid CommandAction type.");
        return discriminator.GetString() switch
        {
            "listFiles" => root.Deserialize<ListFilesCommandAction>(options) ?? throw new JsonException("Expected ListFilesCommandAction."),
            "read" => root.Deserialize<ReadCommandAction>(options) ?? throw new JsonException("Expected ReadCommandAction."),
            "search" => root.Deserialize<SearchCommandAction>(options) ?? throw new JsonException("Expected SearchCommandAction."),
            "unknown" => root.Deserialize<UnknownCommandAction>(options) ?? throw new JsonException("Expected UnknownCommandAction."),
            _ => throw new JsonException("Unknown CommandAction type \u0027" + discriminator.GetString() + "\u0027.")
        };
    }

    public override void Write(Utf8JsonWriter writer, CommandAction value, JsonSerializerOptions options)
    {
        switch (value)
        {
            case ListFilesCommandAction concrete:
                JsonSerializer.Serialize(writer, concrete, options);
                break;
            case ReadCommandAction concrete:
                JsonSerializer.Serialize(writer, concrete, options);
                break;
            case SearchCommandAction concrete:
                JsonSerializer.Serialize(writer, concrete, options);
                break;
            case UnknownCommandAction concrete:
                JsonSerializer.Serialize(writer, concrete, options);
                break;
            default: throw new JsonException("Unknown runtime type for CommandAction.");
        }
    }
}
