// <auto-generated />
#nullable enable

using System;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace ZYC.Codex.Protocol;

public class AccountJsonConverter : JsonConverter<Account>
{
    public override Account Read(ref Utf8JsonReader reader, Type typeToConvert, JsonSerializerOptions options)
    {
        using var document = JsonDocument.ParseValue(ref reader);
        var root = document.RootElement;
        if (root.ValueKind != JsonValueKind.Object || !root.TryGetProperty("type", out var discriminator) || discriminator.ValueKind != JsonValueKind.String)
            throw new JsonException("Missing or invalid Account type.");
        return discriminator.GetString() switch
        {
            "amazonBedrock" => root.Deserialize<AmazonBedrockAccount>(options) ?? throw new JsonException("Expected AmazonBedrockAccount."),
            "apiKey" => root.Deserialize<ApiKeyAccount>(options) ?? throw new JsonException("Expected ApiKeyAccount."),
            "chatgpt" => root.Deserialize<ChatgptAccount>(options) ?? throw new JsonException("Expected ChatgptAccount."),
            _ => throw new JsonException("Unknown Account type \u0027" + discriminator.GetString() + "\u0027.")
        };
    }

    public override void Write(Utf8JsonWriter writer, Account value, JsonSerializerOptions options)
    {
        switch (value)
        {
            case AmazonBedrockAccount concrete:
                JsonSerializer.Serialize(writer, concrete, options);
                break;
            case ApiKeyAccount concrete:
                JsonSerializer.Serialize(writer, concrete, options);
                break;
            case ChatgptAccount concrete:
                JsonSerializer.Serialize(writer, concrete, options);
                break;
            default: throw new JsonException("Unknown runtime type for Account.");
        }
    }
}
