// <auto-generated />
#nullable enable

using System;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace ZYC.Codex.Protocol;

public class LocalShellActionJsonConverter : JsonConverter<LocalShellAction>
{
    public override LocalShellAction Read(ref Utf8JsonReader reader, Type typeToConvert, JsonSerializerOptions options)
    {
        using var document = JsonDocument.ParseValue(ref reader);
        var root = document.RootElement;
        if (root.ValueKind != JsonValueKind.Object || !root.TryGetProperty("type", out var discriminator) || discriminator.ValueKind != JsonValueKind.String)
            throw new JsonException("Missing or invalid LocalShellAction type.");
        return discriminator.GetString() switch
        {
            "exec" => root.Deserialize<ExecLocalShellAction>(options) ?? throw new JsonException("Expected ExecLocalShellAction."),
            _ => throw new JsonException("Unknown LocalShellAction type \u0027" + discriminator.GetString() + "\u0027.")
        };
    }

    public override void Write(Utf8JsonWriter writer, LocalShellAction value, JsonSerializerOptions options)
    {
        switch (value)
        {
            case ExecLocalShellAction concrete:
                JsonSerializer.Serialize(writer, concrete, options);
                break;
            default: throw new JsonException("Unknown runtime type for LocalShellAction.");
        }
    }
}
