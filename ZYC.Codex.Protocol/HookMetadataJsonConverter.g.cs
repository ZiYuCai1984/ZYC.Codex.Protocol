// <auto-generated />
#nullable enable

using System;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace ZYC.Codex.Protocol;

public class HookMetadataJsonConverter : JsonConverter<HookMetadata>
{
    public override HookMetadata Read(ref Utf8JsonReader reader, Type typeToConvert, JsonSerializerOptions options)
    {
        using var document = JsonDocument.ParseValue(ref reader);
        var root = document.RootElement;
        if (root.ValueKind != JsonValueKind.Object || !root.TryGetProperty("handlerType", out var discriminator) || discriminator.ValueKind != JsonValueKind.String)
            throw new JsonException("Missing or invalid HookMetadata handlerType.");
        return discriminator.GetString() switch
        {
            "agent" => root.Deserialize<AgentHookMetadata>(options) ?? throw new JsonException("Expected AgentHookMetadata."),
            "command" => root.Deserialize<HookMetadataCommand>(options) ?? throw new JsonException("Expected HookMetadataCommand."),
            "mcpTool" => root.Deserialize<HookMetadataMcpTool>(options) ?? throw new JsonException("Expected HookMetadataMcpTool."),
            "prompt" => root.Deserialize<PromptHookMetadata>(options) ?? throw new JsonException("Expected PromptHookMetadata."),
            _ => throw new JsonException("Unknown HookMetadata handlerType \u0027" + discriminator.GetString() + "\u0027.")
        };
    }

    public override void Write(Utf8JsonWriter writer, HookMetadata value, JsonSerializerOptions options)
    {
        switch (value)
        {
            case AgentHookMetadata concrete:
                JsonSerializer.Serialize(writer, concrete, options);
                break;
            case HookMetadataCommand concrete:
                JsonSerializer.Serialize(writer, concrete, options);
                break;
            case HookMetadataMcpTool concrete:
                JsonSerializer.Serialize(writer, concrete, options);
                break;
            case PromptHookMetadata concrete:
                JsonSerializer.Serialize(writer, concrete, options);
                break;
            default: throw new JsonException("Unknown runtime type for HookMetadata.");
        }
    }
}
