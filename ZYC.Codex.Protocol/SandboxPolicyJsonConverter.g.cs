// <auto-generated />
#nullable enable

using System;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace ZYC.Codex.Protocol;

public class SandboxPolicyJsonConverter : JsonConverter<SandboxPolicy>
{
    public override SandboxPolicy Read(ref Utf8JsonReader reader, Type typeToConvert, JsonSerializerOptions options)
    {
        using var document = JsonDocument.ParseValue(ref reader);
        var root = document.RootElement;
        if (root.ValueKind != JsonValueKind.Object || !root.TryGetProperty("type", out var discriminator) || discriminator.ValueKind != JsonValueKind.String)
            throw new JsonException("Missing or invalid SandboxPolicy type.");
        return discriminator.GetString() switch
        {
            "dangerFullAccess" => root.Deserialize<DangerFullAccessSandboxPolicy>(options) ?? throw new JsonException("Expected DangerFullAccessSandboxPolicy."),
            "externalSandbox" => root.Deserialize<ExternalSandboxSandboxPolicy>(options) ?? throw new JsonException("Expected ExternalSandboxSandboxPolicy."),
            "readOnly" => root.Deserialize<ReadOnlySandboxPolicy>(options) ?? throw new JsonException("Expected ReadOnlySandboxPolicy."),
            "workspaceWrite" => root.Deserialize<WorkspaceWriteSandboxPolicy>(options) ?? throw new JsonException("Expected WorkspaceWriteSandboxPolicy."),
            _ => throw new JsonException("Unknown SandboxPolicy type \u0027" + discriminator.GetString() + "\u0027.")
        };
    }

    public override void Write(Utf8JsonWriter writer, SandboxPolicy value, JsonSerializerOptions options)
    {
        switch (value)
        {
            case DangerFullAccessSandboxPolicy concrete:
                JsonSerializer.Serialize(writer, concrete, options);
                break;
            case ExternalSandboxSandboxPolicy concrete:
                JsonSerializer.Serialize(writer, concrete, options);
                break;
            case ReadOnlySandboxPolicy concrete:
                JsonSerializer.Serialize(writer, concrete, options);
                break;
            case WorkspaceWriteSandboxPolicy concrete:
                JsonSerializer.Serialize(writer, concrete, options);
                break;
            default: throw new JsonException("Unknown runtime type for SandboxPolicy.");
        }
    }
}
