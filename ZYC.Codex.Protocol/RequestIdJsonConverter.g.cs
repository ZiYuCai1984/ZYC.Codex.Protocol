// <auto-generated />
#nullable enable

using System;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace ZYC.Codex.Protocol;

public class RequestIdJsonConverter : JsonConverter<RequestId>
{
    public override RequestId Read(ref Utf8JsonReader reader, Type typeToConvert, JsonSerializerOptions options)
    {
        using var document = JsonDocument.ParseValue(ref reader);
        var root = document.RootElement;
        if (root.ValueKind == JsonValueKind.Number)
            return new RequestIdInteger(root.Deserialize<long>(options)!);
        if (root.ValueKind == JsonValueKind.String)
            return new RequestIdString(root.Deserialize<string>(options)!);
        throw new JsonException("Unknown RequestId wire shape or value.");
    }

    public override void Write(Utf8JsonWriter writer, RequestId value, JsonSerializerOptions options)
    {
        switch (value)
        {
            case RequestIdInteger concrete:
                JsonSerializer.Serialize(writer, concrete.Value, options);
                break;
            case RequestIdString concrete:
                JsonSerializer.Serialize(writer, concrete.Value, options);
                break;
            default: throw new JsonException("Unknown runtime type for RequestId.");
        }
    }
}
