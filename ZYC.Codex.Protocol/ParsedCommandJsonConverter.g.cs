// <auto-generated />
#nullable enable

using System;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace ZYC.Codex.Protocol;

public class ParsedCommandJsonConverter : JsonConverter<ParsedCommand>
{
    public override ParsedCommand Read(ref Utf8JsonReader reader, Type typeToConvert, JsonSerializerOptions options)
    {
        using var document = JsonDocument.ParseValue(ref reader);
        var root = document.RootElement;
        if (root.ValueKind != JsonValueKind.Object || !root.TryGetProperty("type", out var discriminator) || discriminator.ValueKind != JsonValueKind.String)
            throw new JsonException("Missing or invalid ParsedCommand type.");
        return discriminator.GetString() switch
        {
            "list_files" => root.Deserialize<ListFilesParsedCommand>(options) ?? throw new JsonException("Expected ListFilesParsedCommand."),
            "read" => root.Deserialize<ReadParsedCommand>(options) ?? throw new JsonException("Expected ReadParsedCommand."),
            "search" => root.Deserialize<SearchParsedCommand>(options) ?? throw new JsonException("Expected SearchParsedCommand."),
            "unknown" => root.Deserialize<UnknownParsedCommand>(options) ?? throw new JsonException("Expected UnknownParsedCommand."),
            _ => throw new JsonException("Unknown ParsedCommand type \u0027" + discriminator.GetString() + "\u0027.")
        };
    }

    public override void Write(Utf8JsonWriter writer, ParsedCommand value, JsonSerializerOptions options)
    {
        switch (value)
        {
            case ListFilesParsedCommand concrete:
                JsonSerializer.Serialize(writer, concrete, options);
                break;
            case ReadParsedCommand concrete:
                JsonSerializer.Serialize(writer, concrete, options);
                break;
            case SearchParsedCommand concrete:
                JsonSerializer.Serialize(writer, concrete, options);
                break;
            case UnknownParsedCommand concrete:
                JsonSerializer.Serialize(writer, concrete, options);
                break;
            default: throw new JsonException("Unknown runtime type for ParsedCommand.");
        }
    }
}
